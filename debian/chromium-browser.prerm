#!/bin/sh

set -e

if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ] ; then
    update-alternatives --remove x-www-browser /usr/bin/chromium-browser
    update-alternatives --remove jolicloud-webapps-engine /usr/bin/chromium-browser
fi
